# The Shards — landing page

A single-page landing site for **The Shards**, a game where any person can create their own MMORPG and players can step into every world that has been forged.

The page is one self-contained `index.html`: styles, fonts and the logo are inlined, so there are no build steps, no dependencies and no external asset requests.
